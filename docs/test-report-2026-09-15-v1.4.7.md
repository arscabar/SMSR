# SMSR v1.4.7 통합 테스트 보고서

## 대상과 판정 기준

- 대상: 도구 실행 시작 실시간 기록, 활성 노드 연결, 대시보드 진행 표시, Windows 설치본
- 판정 기준: Release 빌드, 전체 자체검사, 격리 stdio, 설치 파일 생성과 체크섬 검증이 모두 통과해야 한다.

## 검증 결과

| 검사 | 결과 |
|---|---|
| Release 빌드 | 통과, 경고 0·오류 0 |
| `dotnet test` | 종료 코드 0 |
| Release·publish 앱 전체 자체검사 | 모두 종료 코드 0 |
| 훅 등록 | `PreToolUse`·`PostToolUse` 등록 및 중복 방지 통과 |
| 활동 연결 | 같은 `tool_use_id`의 `TOOL_STARTED`·`TOOL_COMPLETED`가 추적 세션에 저장됨 |
| 대시보드 | 작업 제목·한국어 활동명·진행 경과 시간·SSE 연결 상태 표시 계약 통과 |
| 격리 stdio | protocol `2025-11-25`, 도구 12개 |
| 프로세스 보존 | 검사 전후 기존 PID 6개 유지, 신규 잔류 프로세스 0개 |
| 앱·설치 파일 버전 | `1.4.7.0` |
| 설치 파일 | `SMSR-Setup-1.4.7.0-win-x64.exe`, 63,416,657 bytes |
| SHA-256 | `E3C62BCB317DB2529D2098B7B94A62CF2560F1328F8BD34207CAE8EF22BFAA72` |

## 남은 위험

- 이미 열린 Codex 작업은 새 훅 정의를 다시 읽지 않으므로 설치 후 SMSR·Codex 재시작이 필요하다.
- 설치 파일은 코드 서명되지 않았다.
